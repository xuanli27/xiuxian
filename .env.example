# ==================== 数据库配置 ====================
# 
# 方案1: Vercel Postgres (推荐用于生产环境)
# 从 Vercel Dashboard → Storage → Postgres 复制以下环境变量:
POSTGRES_URL="postgresql://..."
POSTGRES_PRISMA_URL="postgresql://...?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NO_SSL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."

# Prisma 使用的环境变量 (自动引用上面的 Vercel 变量)
DATABASE_URL="${POSTGRES_PRISMA_URL}"      # 连接池 URL (用于查询)
DIRECT_URL="${POSTGRES_URL_NON_POOLING}"   # 直连 URL (用于迁移)

# 方案2: 本地 PostgreSQL
# DATABASE_URL="postgresql://username:password@localhost:5432/xiuxian?schema=public"
# DIRECT_URL="postgresql://username:password@localhost:5432/xiuxian?schema=public"

# 方案3: 其他云服务 (Supabase, Railway, Neon 等)
# Supabase 示例:
# DATABASE_URL="postgresql://postgres.[project]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.[project]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# ==================== NextAuth 认证 ====================
NEXTAUTH_SECRET="your-secret-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (可选)
# GOOGLE_CLIENT_ID="your-client-id"
# GOOGLE_CLIENT_SECRET="your-client-secret"

# GitHub OAuth (可选)
# GITHUB_CLIENT_ID="your-github-client-id"
# GITHUB_CLIENT_SECRET="your-github-client-secret"

# ==================== AI 配置 ====================
# 选择 AI 提供商: openai | google | azure | anthropic
AI_PROVIDER="openai"

# OpenAI / 兼容 OpenAI 格式的服务
OPENAI_API_KEY="your-api-key"
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4o-mini"

# Google Gemini
# GOOGLE_API_KEY="your-google-api-key"
# GOOGLE_BASE_URL=""  # 可选，留空使用默认
# GOOGLE_MODEL="gemini-2.0-flash-exp"

# Azure OpenAI
# AZURE_API_KEY="your-azure-key"
# AZURE_BASE_URL="https://your-resource.openai.azure.com/openai/deployments/your-deployment"
# AZURE_MODEL="gpt-4"

# Anthropic Claude
# ANTHROPIC_API_KEY="your-anthropic-key"
# ANTHROPIC_MODEL="claude-sonnet-4-20250514"

# ==================== Redis 缓存（可选）====================
# 如果不配置，系统将直接查询数据库，不影响功能使用
# REDIS_URL="redis://localhost:6379"
# REDIS_URL="redis://default:password@host:port"  # 远程Redis